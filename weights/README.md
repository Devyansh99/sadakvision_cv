# Weights — Hugging Face (coming soon)

No `*.pt` / `*.pth` binaries in git (they are git-ignored).

Planned HF repo will host:

- `yolov8m_best.pt` (~50MB)
- `yolo26m_best.pt` (~42MB)
- `yolov11n_best.pt` (~5MB)
- `rfdetr_best.pth` (~127MB)

Links will be added here after the HF upload step.
