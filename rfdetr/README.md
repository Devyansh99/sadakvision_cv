# RF-DETR — checkpoints via Hugging Face (coming soon)

Checkpoints (`checkpoint_best_*.pth`, 127–255MB) are git-ignored — GitHub blocks files >100MB.

They will be uploaded to Hugging Face; links go here + in `weights/README.md` after the HF step.
